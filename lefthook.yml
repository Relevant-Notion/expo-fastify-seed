pre-commit:
  parallel: true
  commands:
    format-check:
      run: pnpm format:check
    lint:
      run: pnpm lint

pre-push:
  parallel: true
  commands:
    typecheck:
      run: pnpm typecheck
    test:
      run: pnpm test
